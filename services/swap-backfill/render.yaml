services:
  - type: web
    name: swap-backfill-service
    env: node
    plan: starter
    buildCommand: cd services/swap-backfill && npm install
    startCommand: cd services/swap-backfill && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3002

